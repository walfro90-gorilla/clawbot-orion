# ClawBot — Evolución (huella histórica)

> Timeline de cómo evolucionó la arquitectura. Fechas derivadas del git log.
> Para regenerar/extender: `git log --date=short --pretty=format:'%ad | %s'`.
> Última actualización: 2026-06-27 · 49 commits · `2026-04-12` → `2026-06-27`.

El producto pasó por **3 eras**. La inflexión clave fue el pivote **Playwright headless → extensión Chrome (Smart Hybrid)** por el problema del multi-IP ban.

---

## Era 1 — Playwright / Voyager (abr 2026) · *muerta*

Servidor con Chrome headless (Playwright + stealth) scrapeando LinkedIn directo y leyendo el inbox vía Voyager GraphQL. Orquestado por `scheduler.js` + `worker.js`/`batch.js`/`inbox.js`/`search.js`. Next 14. **Toda esta arquitectura hoy está muerta** (ver `CLAUDE.md` §2), pero fue la base sobre la que se construyó el CRM y la IA.

| Fecha | Hito |
|-------|------|
| 2026-04-12 | **First commit.** Orion (CRM) + Prometheus (worker Playwright). Campañas editables, sensor de calor de cuentas LI. |
| 2026-04-13 | Botones de pausa por campaña + tour. |
| 2026-04-23 | Anti-ban hardening + heartbeat del scheduler. Inbox captura todos los inbound + auto-reply más rápido. Integración Cal.com. |
| 2026-04-24/25 | **Sistema de IA por persona** (voz/tono/empresa por campaña). Detección de leads inbound (califica remitentes desconocidos). Schedule configurable por campaña + fines de semana. Monitor de proxy + warning de país. |
| 2026-04-28/29 | **Endurecimiento de seguridad**: quitar service key hardcodeada, HTTP security headers, firewall, limpieza del historial git. |

---

## Era 2 — El pivote Smart Hybrid (may–jun 2026)

La automatización headless se banea: el login opera desde una IP de datacenter ≠ la del usuario. Solución: una **extensión Chrome MV3 ("Orion Sync")** ejecuta las acciones desde la IP/sesión real del usuario; Prometheus solo orquesta vía `extension_commands` + `extension-bridge.js` (WS). Aquí mueren `worker.js`/`inbox.js`/`scheduler.js` y nace `scheduler-extension.js`.

| Fecha | Hito |
|-------|------|
| 2026-05-02 | Fix `followup.js` (aún era el motor legacy de FU). |
| 2026-05-15 | Proxy server actualizado. |
| 2026-05-17 | Campañas editables + fixes de LinkedIn UI. |
| 2026-05-22 | **Primera extensión Orion Sync** (Google gadget / modelo). |
| 2026-05-24/26/27 | Extensión Orion Sync + integración con el monitor + self-health de Gemini. |
| 2026-06-08 | **Refactor mayor de la extensión Orion Sync** — consolidación del enfoque Smart Hybrid. |

---

## Era 3 — Madurez del producto (jun 2026) · *actual*

Con el Smart Hybrid estable, el foco pasa a features de CRM, configurabilidad, performance y robustez.

| Fecha | Hito |
|-------|------|
| 2026-06-11 | Nuevo usuario + about LinkedIn. |
| 2026-06-12 | **Follow-ups dinámicos 1–20 por campaña** (v0.8). Reemplaza los FU hardcodeados; el paso vive en `leads.followup_step`. Ver `CLAUDE.md` §6. |
| 2026-06-15 | Editor de campañas con confirmación + dirty-tracking. **Pausa de automatización por contacto** + anti-loop de auto-reply. |
| 2026-06-16/17 | Flujos de FU por cuenta. |
| 2026-06-18 | **perf**: reducir write-amplification (orphan_conversations + scheduler_log). |
| 2026-06-20 | **perf**: paralelizar queries del Centro de Control (7 round-trips → 2). |
| 2026-06-24 | Fix webhook Cal.com (no ligaba bookings al lead: uuid pelón vs patrón `LEAD_ID=`). |
| 2026-06-26 | **`middleware.ts` → `proxy.ts`** (deprecación Next 16). `.gitignore` blindado para todos los `.env*`. |
| 2026-06-27 | **Cuarentena de leads** (`/dashboard/quarantine`). Auto-tune de timeouts con cap por-phase. Extensión **v0.9.1**: `runPhase` resistente al throttling de tabs en background. |
| 2026-07-02/03 | **Seguridad InMail** (ext v0.9.10–0.9.13): no enviar InMail a 2do grado ni al destinatario equivocado (`composeInmailRecipientGuard`, shadow-DOM aware). Fix whitespace en `typing_complete` (0.9.10). Fase 0 de auditoría institucional. |

---

## Era 4 — Resiliencia y multi-proveedor (jul 2026) · *actual*

Tres caídas de producción en un día (DB Supabase wedged por inanición de compute Nano/Free, box de Upcloud caído ~23h, y Gemini bloqueado por billing) — **las tres pasaron sin alerta**. La respuesta fue endurecer para que un tropiezo nunca vuelva a cascar en outage, y quitar dependencias de proveedor único. Post-mortem completo en [`docs/postmortem-2026-07-03-db-outage.md`](postmortem-2026-07-03-db-outage.md).

| Fecha | Hito |
|-------|------|
| 2026-07-04 | **LLM multi-proveedor** (`lib/ai-message.js`): `callLLM`/`callLLMJson` con cadena **Groq primario → Gemini fallback → template seguro**. Mata la fragilidad de proveedor único (el billing de Gemini rompió toda la generación). Config por env `LLM_PROVIDERS`/`GROQ_MODEL` (default `llama-3.3-70b-versatile`). |
| 2026-07-04 | **Eficiencia de DB** (post-mortem): caché TTL de config (−cientos de queries/tick), `getEligiblePendingCount` slim, bridge con cliente timeout + poll 3s→10s (−67% carga base). |
| 2026-07-04 | **Calidad IA**: regla anti "título como empresa" en el prompt VIVO (la regex vieja era código muerto); whitelist de títulos insensible a acentos. |
| 2026-07-04 | **Búsqueda yield-aware**: backoff del piso de drought (`campaigns.dry_search_streak`) para no martillar un pool agotado cada 30 min. |
| 2026-07-04 | **Watchdog out-of-band** (`watchdog.js`, reemplaza `heartbeat-check.js`): cada 2 min 24/7, alerta por webhook (nunca vía la DB), dead-man's-switch (Healthchecks.io) para box-down, `pm2 jlist` para crash-loop. Base: `lib/notify-ops.js`. |
| 2026-07-04 | **Resiliencia del run-loop del scheduler**: cancelación real de ticks (AbortSignal de tick en `lib/supabase.js` → mata los "zombie ticks" que martillaban la DB), jitter de boot, backoff exponencial ante fallos, `unhandledRejection` ya no hace crash-loop. |
| 2026-07-04 | **Detección de desconexión → Super DEAD** (`leads.disconnected_at`): cuando un contacto que estuvo conectado nos elimina, el lead pasa a DEAD irreversible en vez de revertir a `invite_sent` (evita re-invitar a quien nos eliminó = riesgo anti-ban/denuncia). Gate fail-closed en todas las rutas de envío. |
| 2026-07-07 | **Warmup cliff + footgun del admin**: `effectiveWarmupCap` (rampa por edad) devuelve `null` al día 30 → una cuenta madura aún en `cold` colapsa a cap 5 (Wal 47d estrangulada 5/día). Fix operativo (promover `warmup_status`→`warming`, cap 5→12) + quitar el reset de `warmup_started_at` en `accounts/page.tsx` (promover desde el admin ya NO reinicia la rampa a día-0 = cap 3). |
| 2026-07-07/08 | **Búsqueda dual Free / Sales Navigator** (`linkedin_accounts.search_mode`, toggle por cuenta): las cuentas Pro prospectan por `/sales/search/people` (pool sin el límite comercial de búsqueda del free) en vez de `/search/results/people/`. **Fase 1** flag + andamiaje scheduler (`6eeadca`). **Fase 2** scraper SalesNav en `content.js` (nombre en `[data-anonymize=person-name]`, v0.9.19). **Invite (v7-A, v0.9.23)**: SalesNav "Conectar" pide EMAIL a 2do-grado frío (gate anti-spam → empuja InMail); en vez de conectar desde SalesNav, la ext **resuelve el `/in/` público** del lead y el bridge persiste `linkedin_url=/in/` → invite + FU van por el **perfil público** (flujo free, sin muro de email). SalesNav = solo BÚSQUEDA. Anti-ban: SalesNav **no** sube el cap de invites — solo alimenta el supply (mata la sequía de cuentas hot). |

| 2026-07-27 | **Búsqueda por empresa real** (ext v0.10.0, `campaign_target_companies`): la lista maestra de empresas apenas se usaba — medido en CAFE 57, 15 de 17 búsquedas salían title-only, 21/182 empresas tocadas, 10% de leads de la lista. Cuatro causas: la válvula anti-sequía **borraba** `search_company_names` en drought (y el modo empresa rinde poco ⇒ drought permanente ⇒ bucle que anulaba la feature); la empresa iba concatenada al keyword (match difuso, sin validar); la geografía rotada peleaba contra la empresa (0 resultados); y un solo índice `%N` para título y empresa (cobertura diagonal, ~76 días por vuelta). Ahora: tabla-cursor por empresa + caché de company URN (acción `resolve_companies`, por lotes), **facet nativo `currentCompany`** en la URL, sin `geoUrn` cuando hay empresa, grupo booleano de títulos (varios puestos por visita) y cursor `last_searched_at nulls first` (vuelta completa antes de repetir). Con empresa se fuerza el buscador free (la URL SalesNav es keywords-only y se comía el scoping). Ver `CLAUDE.md` §5/§7. |

| 2026-08-01→03 | **Hardening company-scoped (ext 0.10.1→0.10.10)** — saga de hard testing en vivo hasta dejar la lista maestra FUNCIONAL: resolver de páginas por scoring (aislar tarjeta → puntuar solo el NOMBRE, jamás la descripción → nombre como FILTRO + tamaño decide → reintento con nombre núcleo → sin fallback genérico), `followers`+`page_title` para auditar, autocuración de empresas sin rendimiento, **recarga remota** (`reload_extension` — se acabó el ↻ manual), invites priorizando leads de la lista, pool viejo fuera del conteo de abastecimiento, **SalesNav con filtro nativo CURRENT_COMPANY** (probado en vivo). **Políglota**: los 3 caminos LLM responden en el idioma del contacto (o lo infieren del perfil en el 1er mensaje) + regla anti-respuesta-genérica cuando el contacto no es fit. Flujo CONGELADO: `docs/company-scoped-flujo.md` + hook de confirmación en `.claude/hooks/`. |
| 2026-08-11 | **Filtro de geo en el ingest** — company-scoped tira el geoUrn de la URL (chocaba con el facet), así que las empresas MULTINACIONALES de la lista (hunter douglas, MOLEX, Siemens) surfaceaban empleados de Brasil/Alemania/India: Andre Croccia (hunter douglas, Sao Paulo) se invitó en Café. `matchesCampaignGeo` (`extension-bridge.js`) descarta al insertar los leads fuera de `search_location` — chokepoint server-side, cubre free Y SalesNav. Conservador (solo país extranjero explícito; ciudad-sin-país se conserva). Limpieza: 69 extranjeros → disqualified, 40 falsos positivos restaurados. Self-check `scripts/test-geo-filter.js`. |
| 2026-08-10 | **Fix scope company-scoped** — el escape title-only (tras `dry_search_streak≥5`) tiraba el filtro de empresa y, en SalesNav (ignora geo), traía otros países: Josh recibió un founder de India (competidor). ELIMINADO: en `companyMode` siempre se busca una empresa de la lista, nunca title-only. Limpieza: 15 leads title-only sin `targetCompany` → disqualified. |
| 2026-08-11 | **Digest diario de conexiones + contact-info (ext 0.10.13)** — primer canal de EMAIL saliente del sistema (`lib/send-email.js`, Resend API por fetch nativo, no-op sin `RESEND_API_KEY`). Cada mañana (`send_hour`, tz; config en `runtime_config.daily_digest`, editable en `/dashboard/settings` admin-only) el scheduler envía a N destinatarios un email con los contactos que ACEPTARON la invitación desde el último envío (nombre/empresa/cargo/email/tel/URL, agrupado cuenta→campaña). Corre dentro del tick (patrón `runDailyPublishAgents`: fecha + hora local ⇒ catch-up si el server estaba caído); high-water mark de `connected_at` en key separada `daily_digest_state` que solo escribe el scheduler (avanza SOLO tras 2xx ⇒ retry con `notifyOps`). Email/tel se capturan con la acción nueva **`get_contact_info`** (overlay `/overlay/contact-info/` del 1er grado, extracción por mailto:/tel:/headers, no por clases): pase `tryContactInfoScrape` = prioridad más baja del loop per-account, 1/tick, cap 20/día (`contact_info_daily_cap`), nuevos primero; el bridge escribe `leads.contact_info` (jsonb, `{}` = visitado sin datos). Self-check `scripts/test-digest.js`. |
| 2026-08-11 | **Fix contact-info: scrape sacaba 0 de 60 (ext 0.10.14)** — auditoría en vivo: el `get_contact_info` del digest **nunca** extrajo email/tel (0/60), dos bugs. (1) `overlay_not_found` 52% (Wal 80%, Café 75%, Josh 27%): el hard-load a `/overlay/contact-info/` no montaba el modal a tiempo en máquinas lentas y el poll de 10s no alcanzaba. (2) 0% extracción aun con modal abierto: `findModal` agarraba CUALQUIER `[role=dialog]` (mensajería/prompts) → contenedor equivocado, vacío. Fix en `scrapeContactInfo` (`content.js`): `findContactRoot` SCOPED (solo el contenedor con mailto/tel, link overlay o heading "información de contacto"; cubre variante full-page) + poll 16s + click-opener fallback con más selectores. `payload.debug:true` → adjunta `_debug` (outerHTML 9KB + counts + headings) para capturar el DOM ground-truth en UN push (el bridge ignora `_debug`). Dos capturas en vivo (Wal, lead León Teja) revelaron el root cause real: el hard-load **no monta modal** (`dialogs:0`) y las clases van ofuscadas → el overlay es **página completa**; el heading renderiza antes que el mailto, así que matchear solo el heading devolvía el `<header>` del título sin el email. Fix final **0.10.16** (`6db8f7e`): `findContactRoot(strict)` acepta solo el ancestro con heading de contacto **Y** datos (mailto/tel/email-text) juntos, el poll espera a que el email renderice. **CONFIRMADO en vivo**: extrajo `betoteja@gmail.com`. Gate `CONTACT_INFO_MIN_VERSION`/`ACTION_MIN_VERSION` → pausa el scrape en versiones viejas. |
| 2026-08-12 | **Digest diario POR-CAMPAÑA** (`campaigns.digest_recipients text[]`) — sobre el digest global existente, cada campaña configura sus propios destinatarios y recibe cada mañana un email con SOLO sus contactos aceptados + email/tel del `contact_info`. Conviven (global = vista admin; per-campaña = cliente). Motor `maybeSendCampaignDigests` (`lib/daily-digest.js`): loop de campañas con recipients, gate `send_hour`/`tz` del global (no exige `daily_digest.enabled`), estado per-campaña en row separado `runtime_config.daily_digest_state_campaigns`, 0 contactos = no envía (no spamea) pero marca intento, try/catch por campaña + guard en memoria anti doble-envío. UI: campo "Emails del digest diario" en editor de campaña + Centro de Control (parser `lib/digest-recipients.ts`). Construido con swarm de agentes (motor ‖ UI). ⚠️ **Hallazgo del test E2E**: `RESEND_API_KEY`/`DIGEST_FROM` NUNCA se configuraron en el `.env` de prod → TODO el digest (global desde 0.10.13 Y per-campaña) ha sido **no-op silencioso**; `sendEmail` retorna `{ok:false}` sin credenciales. El canal de email no envía hasta que se pongan las 2 vars + `pm2 restart prometheus-scheduler`. ⚠️ **Lección UI**: el editor de campaña (`campaign-edit-form.tsx`) trackea "dirty" contra un map `LABELS` hardcodeado — un campo nuevo NO habilita "Guardar" hasta registrarlo ahí (fix `eed210b`). |
| 2026-08-13 | **Invites fantasma por límite semanal (ext 0.10.17 + `invites_paused_until`)** — Josh: 5 días (8→13 ago) de `send_invite` `completed` que LinkedIn NUNCA registró: alcanzó el límite semanal de invitaciones y el modal de límite queda abierto tras el click, pero las 3 rutas de envío retornaban `sent` a ciegas (`sleep` + return, sin post-click check). Dominó: la ventana-boundary de `check_sent_invites` marcaba los fantasmas ausentes del top-10 como **falsos accepts** → FU a no-conexiones → churn `detected_not_first_degree` y leads en retry-loop quemando quota. Fix: `verifyInviteSent()` post-click en las 3 rutas (confirmación + `detectInviteLimitModal`) → error `weekly_invite_limit`; el bridge pausa SOLO invites 24h (`linkedin_accounts.invites_paused_until`, `getEffectiveDailyCap`→0; FU/inbox siguen) + `account_alert`, auto-probe al expirar. Data-fix: Josh pausado hasta 16-ago, 11 fantasmas + 3 falsos awaiting → `scraped`. Lección: **todo reporte de éxito de la ext necesita verificación post-acción; el ground truth es la página de LinkedIn, no el click.** |
| 2026-08-13/14 | **Agente de retiro de invitaciones viejas (ext 0.10.18→0.10.25) — hard-test nocturno en vivo**. Objetivo: drenar el backlog de pending (Josh 492 / Wal 271) que endurece los límites. La ruta obvia (lista `/sent/`, click "Retirar") resultó **imposible de automatizar**: el SDUI nuevo exige eventos `isTrusted` — 4 iteraciones de click sintético fallaron y solo el click real la activó (retiro manual validado: 271→270). Pivote a **retiro vía perfil DB-first** (`withdraw_invite_profile`): candidatos desde `leads` (invite_sent >30d), `/in/<lead>` → anchor "Pendiente" → modal → Retirar; 1/tick, cap 15/día, retirado → `dead(invite_withdrawn)` (bloqueo LinkedIn ~3 semanas re-invite), errores → cooldown 7d. Botín lateral del hard-test: el scroller de `/sent/` es `<main>` (fix compartido a `checkSentInvites` → accept-detection puede cargar la lista completa, antes ~10/N); lazy-load requiere tab visible (IntersectionObserver); "Pendiente"/"Retirar" son `<a>` no `<button>`; el sidebar de sugeridos contamina heurísticas de botones; `reload_extension` mata el WS (no encolar con otros comandos). **Veredicto final (14-ago, 16 runs en producción): LinkedIn exige `isTrusted` también en el perfil** (Café encontró el `<a>` Pendiente y el click sintético fue ignorado ×9; Josh ni renderiza el control) → **pase deshabilitado** (gate `9.9.9`); código queda para plan B (chrome.debugger vía optional_permissions) o retiro manual mensual. |
| 2026-08-14/15 | **Los accepts pasan a detección POSITIVA (ext 0.10.26→0.10.28)** — investigar por qué Josh no invitaba destapó algo mayor: LinkedIn **descarta en silencio los envíos automatizados de esa cuenta** mientras acepta los suyos manuales (experimento controlado 14-ago 23:11Z: la invitación de Café por Orion queda al tope de sus 212/212; la de Josh no existe; la manual de Josh sí). El cierre del modal nunca probó envío → `send_invite` ahora reporta `confirmedBy: 'toast' \| 'modal_closed'` (+ captura de modal con `payload.debug`). El daño real venía del amplificador: `ingestCheckSentInvites` infería "aceptó" de la AUSENCIA en `/sent/`, así que cada invite fantasma se volvía un falso `connected` (Josh: 76 FU a no-conexiones en 2 días, 119 leads con flag). **Ahora los accepts los decide solo `check_connections` por presencia**, con red de seguridad: si esa cuenta no tuvo un scan de conexiones con datos en 24h, se conserva la inferencia conservadora (dejar accepts reales sin FU es el fallo caro, caso 8-jun). Habilitador imprescindible: el `check_connections` de Josh **timeouteaba siempre** (120s → 0 conexiones, detección positiva muerta hacía semanas) → presupuesto de 75s entregando lo acumulado, guard de visibilidad y foco de tab para `check_connections`/`check_sent_invites` (sin foco el IntersectionObserver no dispara: Café había caído de 136 a 10 conexiones). Contención de la cuenta: `invites_paused_until` de Josh hasta 22-ago. |
| 2026-08-12→15 | **LinkedIn quita el URN del buscador de empresas → última puerta al title-only cerrada** (`ecdad90`). Corte medible el 12-ago: los comandos `resolve_companies` pasan de traer URN a `urn:null` en el 100% de los candidatos, en las 3 cuentas, con el resolver matcheando bien (`matched:true` + slug + followers, `contentVersion` 0.10.10 = no es código viejo): cambió el markup de `/search/results/companies/` y `data-chameleon-result-urn` ya no existe. Dominó: `ingestResolveCompanies` exigía `urn && matched` → las 42 empresas de la campaña nueva (Aduanas Infinity, cuenta Rosy) se quedaron en `pending` → `pickNextTargetCompany` (solo mira `ready`/`unresolved`) devolvía `null` → **la búsqueda salía title-only igual** pese al fix del 10-ago: 25 leads, CERO de la lista (6 invitados a MAHLE/Grupo Nexos/Serviacero, 19 en cola incluidas agencias aduanales = competencia del cliente). Fix: (a) `matched && !urn` degrada YA a `unresolved` (búsqueda por `"nombre exacto"`, on-list) en vez de esperar 3 rondas de 24h; (b) `companyMode && !target` ⇒ `skipped: 'no_company_target'`, nunca title-only. **Consecuencia viva: toda empresa NUEVA de cualquier cliente entra degradada** (sin facet `currentCompany` ni CURRENT_COMPANY de SalesNav) hasta que la ext recupere el URN navegando a `/company/<slug>/`. Lección: un flujo congelado se rompe sin tocar código porque la dependencia real es el DOM de LinkedIn — cada dato extraído del markup necesita su query de auditoría (`docs/company-scoped-flujo.md`). |
| 2026-08-17 | **Outage de PostgREST: el Free tier deja de dar (`b9b8cba`)** — 13:15 UTC Postgres empieza a cancelar statements, 13:28 deja de loggear y durante ~1h50 el proyecto entero devuelve `522`; después `503 PGRST002` ("could not query the database for the schema cache"). Efecto: el bridge no puede leer `extension_api_key` (query >20s ⇒ timeout ⇒ `db: <empty>`) y las 4 cuentas caen en `auth_locked_5min` en cascada; el scheduler muere por `tick timeout` y el watchdog lo reinicia en bucle. **Orion web nunca se cayó** (307 en 17ms local) — el síntoma "Orion tumbado" era la DB. Diagnóstico por descarte: cuotas sanas (DB 56%, egress 15% — el Usage Summary NO mide compute), credenciales correctas (la key de la ext coincide con la de la DB), **Postgres directo rápido** (22 conexiones, 1 activa, 0 idle-in-tx) y esquema normal (95 tablas / 1.228 columnas) ⇒ la capa rota era **PostgREST**, no la base. Recuperación sin restart del proyecto ni `pause`/`restore` (en Free restauraría desde backup = pérdida de datos): parar `prometheus-scheduler`+`extension-bridge` dos veces hasta que PostgREST volviera solo (15:08). Se reproduce el patrón *sana en reposo, se rompe con carga*. Mitigación: poll del bridge **10s → 30s** (−67% del piso de queries, misma palanca que el 3s→10s del 3-jul), con el postmortem escrito junto al anterior en `extension-bridge.js`. Carga medida: ~13k req/h, de ellas 6k a `/rest/v1/extension_commands`. **Lección: el fix de raíz no es un número de intervalo — es el compute.** Segunda caída por la misma causa; el margen que compró el 3-jul duró seis semanas. **Resiliencia (`b3e922e`)**: el apagón se alargó por amplificación propia, dos defectos con la misma raíz —*un fallo de infraestructura no es un fallo de datos*—: (1) el `error` de la query de auth se descartaba ⇒ un timeout de 20s caía en la misma rama que una key equivocada ⇒ las 4 cuentas a `auth_locked_5min` en cascada culpando a credenciales correctas; ahora cierra con `4006` **sin** `auth_error` (que el cliente convertiría en su propio lockout de 5min) y reconecta en ~2s; (2) nada frenaba a los pollers contra una API muerta —hubo que parar el proceso a mano ×2 para que levantara— ahora `lib/db-circuit.js` abre el circuito a los 3 fallos y pausa poll+cleanup 60s (con self-check: si se abriera de más, el bridge dejaría de despachar para siempre). Bonus: el mismo patrón escondía un **fail-open** — una query fallida de cuentas pausadas se leía como "ninguna pausada" y despachaba a cuentas pausadas. Y la detección ya funcionaba: el watchdog avisó a las 13:16, 1 min después del corte, 60 veces; lo que faltaba era **la suscripción al topic ntfy `clawbot-alertas-orion`**. **Y el CPU al 99% no era solo el tier**: el reparto de `pg_stat_statements` destapó que el 22,3% se lo llevaba `/api/extension/account-status` haciendo Seq Scan de `extension_commands` para pedir 1 fila (índice nuevo `idx_extcmd_account_completed_at`: buffers 315→3) y otro 5,6% la vista `v_crm_lead_list`, cuyos 3 CTE agregaban sobre las tablas ENTERAS antes de filtrar por cuenta — para devolver 25 filas calculaba los contadores de los 1.492 leads; pasados a `LEFT JOIN LATERAL` (índices ya existentes) el listado del CRM bajó de **663 ms a 29 ms**. El 8,6% restante era la introspección de esquema de PostgREST a **14,5 s por llamada**: la misma operación que reventó como `PGRST002`. Lección doble: **el tier pone el techo y la carga ineficiente sube el suelo — mirar el reparto antes de culpar al plan**; y para tocar una vista viva, vista candidata + `EXCEPT` en ambos sentidos + `CREATE OR REPLACE` (nunca `DROP`, conserva los GRANT). |
| 2026-08-17 | **Primer email que llega a los clientes** (`a5228ee`, `fe4ef22`) — desde el 12-ago los digests per-campaña daban **Resend 403** (`from=onboarding@resend.dev` = modo test, solo entrega al dueño de la cuenta): 3 de las 4 campañas con destinatarios **no habían enviado ni uno**, y la alerta que lo destapó solo se vio cuando el operador se suscribió al canal. Cerrado en el día: `ebooms.com` verificado en Resend con los 4 registros DNS creados por AWS CLI en Route 53 (zona `Z03150043832D3KGPJIWH`; valores sacados de la **API de Resend**, no de la UI que los trunca), remitente `Orion Lead Connections <digest@ebooms.com>`, y **`DIGEST_REPLY_TO`** nuevo — sin él, un cliente que contesta *"me interesa"* se come un rebote, el peor fallo en un canal cuyo único fin es abrir conversaciones. La marca "ClawBot" era visible en **4 sitios** (2 asuntos, `<h1>` y pie), no solo el remitente. Se descartó a propósito `campaigns.digest_from`: con una sola marca para todos los clientes sería un env var disfrazado de columna. Infra nueva de la casa: AWS CLI + MCP `aws-mcp` con usuario IAM `clawbot-dns` acotado a 5 acciones de Route 53. |
| 2026-08-17 | **El auto-reply aprende a QUIÉN le responde, y la cadena de LLM se cae entera** (`b475120`, `8d41299`, `fafa9ac`) — dos mensajes malos de la cuenta Wal destaparon dos capas distintas. (1) A un lead le llegó literalmente **`"User Safety: safe."`**: el veredicto del clasificador interno del modelo en vez del mensaje, enviado crudo porque pasaba los 3 guards (sin placeholders, ≤maxChars, termina en punto). Nuevo `isMetaOutput` en `callProvider` —chokepoint único de invite/FU-LLM/FM— prefijo meta o <30 chars ⇒ retry ⇒ error ⇒ template verbatim; **NO** se pone en el guard pre-envío del FU porque ahí pasa el verbatim y un template corto legítimo quedaría bloqueado en silencio. Caso único en todo el historial. (2) El FM asumía "prospecto" para TODO inbound: a un reclutador técnico le **inventó experiencia laboral** del dueño de la cuenta, a quien pidió el portafolio se lo negó, y dejó a una reclutadora esperando 3 respuestas de filtro 2 días. `classifyInboundRole` (clasificador aparte del sensor de salida, fail-open a `prospect`) + `inboundRoleBlock` inyectado **al final** del prompt para ganarle al pitch de la campaña; `recruiter`/`vendor` nunca reciben el cal.com. **Decisión del operador: a un reclutador NO le contesta el bot** — `automation_paused` + alerta `manual_reply_needed`; el camino automático (con CV/portafolio/GitHub desde `account_config.recruiter_reply`) queda tras un `auto_reply:true` apagado. Investigar el envío destapó lo tercero: **los 3 proveedores de LLM fallaban a la vez** y degradaban en silencio (FU a template literal, FM sin responder: 51 replies sin contestar, 1 lead muerto por `gemini_unreachable_after_3_retries`) — Groq **retiró `llama-3.3-70b-versatile`** (su 404 dice "*or you do not have access*" y hace diagnosticar tier cuando la key es de PAGA: los headers `x-ratelimit-limit-requests: 500000` lo zanjan), OpenRouter free agotado y Gemini fuera de la cadena (y de todos modos free = 20 req/día + `FAILED_PRECONDITION` por región desde el datacenter). Prod pasa a `GROQ_MODEL=openai/gpt-oss-120b` y se suma **Kimi/Moonshot como respaldo también de pago** (`LLM_PROVIDERS=groq,moonshot,gemini,openrouter`), con dos ajustes por proveedor nuevos en `OPENAI_COMPAT`: `fixedTemperature: 1` (kimi da 400 con cualquier otro valor) y `tokenMultiplier: 4` (sus tokens de razonamiento se comen el cap ⇒ JSON truncado). Lección: **el nombre de un modelo caduca sin avisar y la degradación es silenciosa** — verificar con `/v1/models` antes de culpar al prompt o a la key. |
| 2026-08-06→09 | **Anti-ban y operación (ext 0.10.11)** — focus-hack anti-throttle DESACTIVADO y validado innecesario (4 dry-runs sin foco + días de envíos reales, 0 typing_timeout; Chrome ya no throttlea con WS del bridge activo). **Piso de invite POR-CUENTA** (`account_invite_floor_not_met`, 6min): el gap era por-campaña y 2 campañas podían disparar en el mismo tick (medido: 2 invites a 4s = patrón bot) → ahora imposible <6min entre invites de una cuenta. **Cerebro entrenable desde screenshots** (`/api/cerebro/analyze-screenshot`, Gemini Vision → borrador de playbook, humano aprueba) + **modo propuesta** (rol `user` propone entradas inactivas por-campaña, admin activa). Fix warmup: cuenta nueva nace `cold` con la rampa corriendo desde su creación. Hallazgo abierto: `check_inbox` de Wal falla ~19% (`conversations_container_not_found`) — ambiental (laptop del usuario), lo cierra el host dedicado. |

---

## Estado de versiones (referencia rápida)

- **Extensión Orion Sync**: `v0.10.28` (manifest) — 0.10.13→0.10.16 arreglan el scrape de contact-info (el overlay es página completa, no modal); **0.10.17** `verifyInviteSent` (fin de los invites reportados a ciegas); 0.10.18→0.10.25 la saga del retiro (descartado: LinkedIn exige `isTrusted`); **0.10.26/27** telemetría `confirmedBy` + captura de modal con `payload.debug`; **0.10.28** `check_connections` con presupuesto de 75s (antes timeouteaba entero en cuentas grandes) + foco de tab para `check_connections`/`check_sent_invites` (el lazy-load por IntersectionObserver no corre en segundo plano). Focus-hack: ON para search/withdraw/las dos listas; OFF para invite/FU/inbox. ⚠️ verificar versión real con `linkedin_accounts.ext_version` — cada máquina actualiza con su propio installer (`reload_extension` NO descarga el bundle). Al 15-ago: Wal 0.10.28, Josh/Café/Rosy 0.10.27.
- **Motor de follow-ups**: v0.8 (dinámico, `campaign_followups`).
- **Orion**: Next 16.2.3 · React 19.
- **Arquitectura**: Smart Hybrid (extensión + bridge WS). Playwright/Voyager = muerta.

> Cómo mantener este archivo: cuando aterrice un hito de arquitectura, añade una fila con la fecha del commit. No documentes cada commit — solo inflexiones (cambio de arquitectura, motor nuevo, migración de framework, feature estructural).
